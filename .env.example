# Odyssey HUD 2026 - Environment Variables Template
# =================================================
# Copy this file to .env and fill in your actual values
# DO NOT commit .env to version control

# Supabase Database URL (for Prisma ORM)
# Format: postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres
# Get from: Supabase Dashboard > Project Settings > Database > Connection String > URI
DATABASE_URL="postgresql://postgres:[YOUR-PASSWORD]@db.[PROJECT-REF].supabase.co:5432/postgres"

# Supabase Project URL (public, exposed to client)
# Get from: Supabase Dashboard > Project Settings > API > Project URL
NEXT_PUBLIC_SUPABASE_URL="https://[PROJECT-REF].supabase.co"

# Supabase Anon Key (public, for client-side Supabase client with @supabase/ssr)
# Get from: Supabase Dashboard > Project Settings > API > anon/public key (Legacy tab)
# OR: Supabase Dashboard > Project Settings > API > Publishable key (new format: sb_publishable_xxx)
# SAFE to expose publicly when Row Level Security (RLS) is enabled
NEXT_PUBLIC_SUPABASE_ANON_KEY="[YOUR-ANON-KEY-OR-PUBLISHABLE-KEY]"

# Supabase Service Role Key (server-side only, NEVER expose to client)
# Get from: Supabase Dashboard > Project Settings > API > service_role (secret)
# WARNING: This key bypasses RLS and has full database access
SUPABASE_SERVICE_ROLE_KEY="[YOUR-SERVICE-ROLE-KEY]"

# Application URL (for auth redirects)
# Development: http://localhost:3000
# Production: https://your-domain.com
NEXT_PUBLIC_APP_URL="http://localhost:3000"
